# Core
pandas>=2.0
numpy>=1.24
requests>=2.31
python-dotenv>=1.0
pulp>=2.7
scikit-learn>=1.4
joblib>=1.3
# Will add as the project grows:
# scikit-learn      (V2 regression models)
# xgboost / lightgbm / catboost   (V4 models)
# pulp / ortools    (squad optimizer, Phase 4)
# streamlit         (dashboard, Phase 9)
# pytest            (tests)
